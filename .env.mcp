# ================================================
# MCP (Model Context Protocol) API Keys
# ================================================
# Created: September 2025
# These are required for the enhanced MCP servers configured in .bmad-core/mcp-servers.json

# -------------------- MUST-HAVE MCPs --------------------

# GitHub MCP - OAuth 2.1 + PKCE authentication
# Get from: https://github.com/settings/tokens
# Required scopes: repo, workflow, read:org
GITHUB_PERSONAL_ACCESS_TOKEN=

# PostgreSQL MCP - Database operations
# Format: postgresql://username:password@host:port/database
# Example: postgresql://user:pass@localhost:5432/gangrun_db
POSTGRES_CONNECTION_STRING=

# -------------------- HIGH-VALUE PRODUCTIVITY MCPs --------------------

# Exa MCP - Advanced web search
# Get from: https://exa.ai (sign up for API access)
EXA_API_KEY=

# Firecrawl MCP - Web scraping and data extraction
# Get from: https://firecrawl.dev (create account for API key)
FIRECRAWL_API_KEY=

# Notion MCP - Document and workspace integration
# Get from: https://www.notion.so/my-integrations
# Create an integration and get the API key
NOTION_API_KEY=

# Slack MCP - Team communication automation
# Get from: https://api.slack.com/apps (create a new app)
# Bot User OAuth Token (starts with xoxb-)
SLACK_BOT_TOKEN=
# App-Level Token (starts with xapp-)
SLACK_APP_TOKEN=

# -------------------- OPTIONAL MCPs --------------------

# Selenium MCP - Browser automation
# Note: Selenium uses local browser drivers, no API key needed
# But you can specify browser preferences:
SELENIUM_BROWSER=chrome
SELENIUM_HEADLESS=false

# -------------------- NOTES --------------------
# 1. Copy this file to .env and fill in the actual API keys
# 2. Never commit .env file with actual keys to version control
# 3. Add .env to .gitignore if not already there
# 4. Some MCPs (filesystem, memory, git, etc.) don't require API keys
# 5. Status in mcp-servers.json:
#    - "active" = ready to use without API keys
#    - "configured" = requires API key from this file

# -------------------- SECURITY REMINDER --------------------
# MCP security landscape is evolving rapidly with new vulnerabilities
# emerging monthly. Prioritize security with defense-in-depth strategies:
# - Rotate API keys regularly
# - Use environment-specific keys (dev/staging/prod)
# - Monitor API usage for anomalies
# - Implement rate limiting where possible